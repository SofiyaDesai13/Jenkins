pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        echo "Hello I m in build"
      }
    stage('test') {
      steps {
        echo "Hello I m in Test"
      }
    stage('deploy') {
      steps {
        echo "Hello I m in deploy"
      }
    }
  }
}
